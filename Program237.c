
#include<stdio.h>

int main()
{
	char ch='A';
	for(int i=0;i<100;i++,ch++)
	{
		printf("ASCII value of is %c and %d\n",ch,ch);
	}
	//printf("Enter character that we want to find the ASCII value");
	//printf("Enter character that we want to find the ASCII value");
	//scanf("%c",&ch);
	printf("answer of above question is %d",ch);	
	return 0;
}